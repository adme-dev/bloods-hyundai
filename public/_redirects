# Netlify _redirects file
# https://docs.netlify.com/routing/redirects/
# Note: Nuxt's Netlify preset handles routing. Keep this file minimal.

# DO NOT add www to non-www redirect here - it causes redirect loops with SSR
# Configure domain redirects in Netlify Dashboard > Domain settings instead

# Legacy URL redirects (add as needed)
# /old-path /new-path 301
