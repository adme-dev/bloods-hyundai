# Netlify _redirects file
# https://docs.netlify.com/routing/redirects/
# Note: Nuxt's Netlify preset auto-generates SSR routing rules
# These rules are for additional custom redirects only

# Redirect www to non-www (canonical URL)
https://www.salehyundai.com.au/* https://salehyundai.com.au/:splat 301!

# Legacy URL redirects (add as needed)
# /old-path /new-path 301
