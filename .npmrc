# NPM configuration for cross-platform builds
# See: https://github.com/npm/cli/issues/4828

# Allow legacy peer deps for compatibility
legacy-peer-deps = true

# Ensure optional dependencies are installed
optional = true
